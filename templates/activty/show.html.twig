{% extends 'base.html.twig' %}

{% block title %}Activty{% endblock %}

{% block body %}
    <h1>Activty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activty.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activty.name }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ activty.subject }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ activty.text }}</td>
            </tr>
            <tr>
                <th>ImageFileName</th>
                <td>{{ activty.imageFileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activty_index') }}">back to list</a>

    <a href="{{ path('app_activty_edit', {'id': activty.id}) }}">edit</a>

    {{ include('activty/_delete_form.html.twig') }}
{% endblock %}
